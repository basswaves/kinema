# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Native playback libraries — ~95MB, fetched by `npx tauri-plugin-libmpv-api setup-lib`
src-tauri/lib/

# Rust build output
src-tauri/target/

# Portable app folder assembled by `npm run app:build` — the exe plus ~95MB of
# native libraries, all of it reproducible from source.
dist-app/

# Diagnostic logs
mpv.log
src-tauri/mpv.log
app.log
src-tauri/app.log

# Local secrets (TMDB / OMDb API keys)
.env
.env.local

# Test media. Clips kept around to exercise a specific decode path — the
# interlaced one proves `deinterlace=auto` engages — but they are megabytes of
# binary that nothing builds from, so they stay out of history.
/*.mp4
/*.mkv
/*.m2ts
